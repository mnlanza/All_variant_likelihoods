options(vsc.use_httpgd = TRUE)

if (file.exists("~/.Renviron")) {
    readRenviron("~/.Renviron")
}
options(repos = c(CRAN = "https://lib.stat.cmu.edu/R/CRAN/"))

# Disable language server diagnostics
options(languageserver.diagnostics = FALSE) 